// The church's calendar is kept in Outlook set to US Eastern time, so every
// date/time shown on the site is normalized to this zone (it automatically
// accounts for the EST/EDT switch) rather than whatever timezone the server
// or visitor's browser happens to run in.
export const CHURCH_TIME_ZONE = "America/New_York";
